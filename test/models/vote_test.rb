require 'test_helper'

class VoteTest < ActiveSupport::TestCase

  describe "Voting" do
    it "has a user"
    it "has a question"
    it "belongs to a poll"
    it "does not allow a user to vote on the same poll twice!"
  end
end
